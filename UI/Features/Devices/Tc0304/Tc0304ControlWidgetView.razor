@using UI.Backend.ViewModels.Tc0304
@inherits ReactiveUI.Blazor.ReactiveComponentBase<Tc0304UnitControlViewModel>

<div class="d-flex justify-content-between gap-5">
  <div>
    <div class="name-display">@ViewModel!.Probe1Name</div>
    <div class="temperature-display">@(ViewModel!.T1Temp?.ToString() ?? "--")</div>
    <div class="name-display">@ViewModel!.Probe3Name</div>
    <div class="temperature-display">@(ViewModel!.T3Temp?.ToString() ?? "--")</div>
  </div>
  <div>
    <div class="name-display">@ViewModel!.Probe2Name</div>
    <div class="temperature-display">@(ViewModel!.T2Temp?.ToString() ?? "--")</div>
    <div class="name-display">@ViewModel!.Probe4Name</div>
    <div class="temperature-display">@(ViewModel!.T4Temp?.ToString() ?? "--")</div>
  </div>
</div>

@code {

}