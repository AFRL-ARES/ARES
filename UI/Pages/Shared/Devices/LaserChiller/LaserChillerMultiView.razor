@using DynamicData.Binding;
@using UI.Backend.ViewModels.Devices.LaserChiller;
@inherits ReactiveUI.Blazor.ReactiveInjectableComponentBase<LaserChillerMultiViewModel>

@foreach (var device in ViewModel!.ConnectedSerialDeviceUnitControlVms)
{
  <div class="d-flex flex-wrap">
    <DashboardWidget>
      <LaserChillerControlWidgetView ViewModel="@device" />
    </DashboardWidget>
  </div>
}

@code {
  protected override Task OnInitializedAsync()
  {
    ViewModel!.ConnectedSerialDeviceUnitControlVms.ObserveCollectionChanges().Subscribe(_ => InvokeAsync(StateHasChanged));
    ViewModel.Start(TimeSpan.FromSeconds(5));
    return base.OnInitializedAsync();
  }

}