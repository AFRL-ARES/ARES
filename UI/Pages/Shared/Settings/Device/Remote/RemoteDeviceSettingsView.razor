@using System.Reactive.Linq
@using System.Reactive.Threading.Tasks
@using Ares.Datamodel
@using UI.Backend.ViewModels.Settings.Device.Remote
@inherits ReactiveUI.Blazor.ReactiveComponentBase<RemoteDeviceSettingsViewModel>
@inject DialogService DialogService

<DeviceSettingsContainer EditCallback="EditCallback" RemoveCallback="RemoveCallback" RefreshCallback="" Name="@ViewModel!.Name" State="@ViewModel.OperationalState" StateMessage="@ViewModel.StateMessage" IsBusy="@ViewModel.IsBusy">
  <div style="display: flex; flex-direction: column">
    <span>Address: @ViewModel.Address</span>
    <span>Type: @ViewModel.Type</span>
    <span>Version: @ViewModel.Version</span>
    <span>@ViewModel.Description</span>
    <div style="display: flex; flex-direction: column">
      @foreach (var settingField in ViewModel.Settings.Fields)
      {
        <AresValueDesigner Name="@settingField.Key" Value="@settingField.Value" Schema="@ViewModel.SettingsSchema.Fields[settingField.Key]" ValueEditCallback="@(value => ValueEditCallback(settingField.Key, value))" />
      }
    </div>
  </div>
</DeviceSettingsContainer>

@code {
  private async Task EditCallback()
  {
    var viewModel = ViewModel!.EditViewModel;
    var result = await DialogService.OpenAsync("Edit Device", ds =>
      @<RemoteDeviceConfigEditView ViewModel="@viewModel" OnValidSubmit="() => ds.Close(true)"/>);

    if (result is true && viewModel.Modified)
    {
      await ViewModel.SaveCommand.Execute();
    }
  }

  private async Task RemoveCallback()
  {
    var result = await DialogService.Confirm($"Delete {ViewModel!.Name}?");
    if (result is true)
    {
      await ViewModel!.RemoveCommand.Execute();
    }
  }

  private async Task RefreshCallback()
  {
    await Task.WhenAll(
      ViewModel!.UpdateInfoCommand.Execute().ToTask(),
      ViewModel!.UpdateStateCommand.Execute().ToTask(),
      ViewModel!.FetchSettingsCommand.Execute().ToTask());
  }

  private Task ValueEditCallback(string name, AresValue value)
  {
    ViewModel!.Settings.Fields[name] = value;
    return ViewModel.PushSettingsCommand.Execute().ToTask();
  }
}
